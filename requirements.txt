# H-SFP / E-HSFP dependencies.
# Tested with Python 3.9-3.11, PyTorch 2.7 + CUDA 12.8.
# For a CUDA build of torch, install from the official index first, e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
# then:  pip install -r requirements.txt

# ---- Core ----
torch>=2.0
torchvision>=0.15
numpy>=1.24
scipy>=1.10
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.6
Pillow>=9.0
tqdm>=4.62
psutil>=5.9
tensorboardX>=2.5
PyYAML>=6.0
kornia>=0.7          # GPU SSL augmentations (classification/H-SFP/hierarchy.py)

# ---- Optional ----
wandb                # experiment tracking (main.py --wandb)
seaborn              # plotting scripts in tools/
scikit-image         # SSIM metric in camera-ready inversion analysis
lpips                # LPIPS metric in camera-ready inversion analysis (optional)
