# Camera-ready SMOKE config for HierFL (CIFAR-100, ResNet50). NOT for paper numbers.
base: ../../classification/hierfl/cifar_hierfl_resnet50.yaml

num_users: 8
mid_server: [2]
epochs: 2
frac: 1.0
local_ep: 1
local_bs: 16
