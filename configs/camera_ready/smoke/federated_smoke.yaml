# Camera-ready SMOKE config for Federated/FedAvg (CIFAR-100, ResNet50). NOT for paper numbers.
base: ../../classification/federated/cifar_fedavg_resnet50.yaml

num_users: 8
epochs: 2
frac: 1.0
local_ep: 1
local_bs: 16
