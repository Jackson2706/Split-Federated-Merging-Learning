# MNIST Centralized Configuration
dataset: mnist
model: cnn
input_channels: 1
output_channels: 10
batch_size: 128
num_clients: 1  # Centralized learning
lr: 0.01
num_communication: 100
momentum: 0.9
weight_decay: 0.0

# Inherit other settings from base_config.yaml 